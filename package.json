{
  "scripts": {
    "dev": "vite",
    "generateFrames": "node scripts/build.cjs",
    "prebuild": "npm run format",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint src",
    "format": "prettier -w src",
    "deploy": "git subtree push --prefix build origin gh-pages"
  },
  "devDependencies": {
    "@types/eslint": "^9.6.1",
    "@types/node": "^24.5.2",
    "eslint": "^9.36.0",
    "globals": "^16.4.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.44.0",
    "vite": "^7.1.6"
  },
  "dependencies": {
    "jimp": "^1.6.0"
  }
}
